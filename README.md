# composer
Minimalistic GUI key composer

# dependencies
libgtk-3-dev
libxdo3 from [xdotool](https://github.com/jordansissel/xdotool)

# install and run
```shell
meson build
cd build
ninja && ./composer
```

